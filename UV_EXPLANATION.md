# UV映射（UV Mapping）详解：从原理到生成逻辑

## 什么是 UV？

在计算机图形学（3D建模和游戏开发）中，**UV** 并不是什么神秘的缩写，它其实代表的是**二维坐标系中的两个轴**。
由于我们在 3D 空间中已经使用了 X、Y、Z 这三个字母来表示三维坐标（也就是物体的长、宽、高），为了避免混淆，在表示 **2D 纹理贴图（图片）** 上的坐标时，就使用了紧挨着 X、Y、Z 的前面两个字母：**U 和 V**。
- **U**：代表水平方向（相当于 2D 坐标系的 X 轴）。
- **V**：代表垂直方向（相当于 2D 坐标系的 Y 轴）。
*(有时候也会有 W 轴，表示 3D 纹理，但在日常开发中 2D 的 UV 是最常见的)。*

---

## UV 的作用是什么？（为什么我们需要 UV？）

一句话概括：**UV 的作用就是告诉电脑，如何把一张平面的 2D 图片（纹理贴图），正确地“贴”到一个立体的 3D 模型表面上。**

为了让你明确知道它的作用，你可以想象以下场景：

### 1. 包装纸与巧克力的比喻
想象你手里有一块形状复杂的巧克力（3D模型），你还有一张印着精美图案的方形糖纸（2D贴图）。你怎么才能让糖纸上的特定图案（比如品牌Logo）正好出现在巧克力的正中心呢？
你需要把这块巧克力的表面“展开”铺平，然后放到糖纸上比划：这个角对应糖纸的左上角，那个面对应糖纸的中心。
**这个“将3D表面铺平并在2D图片上寻找对应点”的过程，建立的关系就是 UV 映射。** UV 坐标记录了模型上的每一个顶点（Vertex）对应贴图上的哪个像素。

### 2. 赋予模型细节与灵魂
如果不使用 UV 和贴图，一个 3D 模型不管建得多精细，它看起来也就是一个灰白色的石膏像。
通过 UV，我们可以给模型穿上衣服：
- **漫反射贴图（Albedo/Diffuse）**：给模型上色（比如画上眼睛、衣服的布料纹理、木头的纹理）。
- **法线贴图（Normal Map）**：不增加模型面数的情况下，通过一张带颜色的图片伪造出凹凸不平的高级细节（比如砖墙的缝隙、盔甲的划痕）。
- **粗糙度/金属度贴图（Roughness/Metallic）**：告诉渲染器模型的哪个部分是光滑的金属，哪个部分是粗糙的泥土。

**所有的这些贴图，都必须依赖 UV 坐标才能准确地贴合在模型对应的位置上。没有 UV，电脑就不知道把图片的哪里画在模型的哪里，贴图就会错乱、拉伸甚至完全无法显示。**

---

## UV 的生成逻辑（它是怎么算出来的？）

在 3D 软件（如 Blender, Maya, 3ds Max）或程序化生成中，模型是由点、线、面构成的。生成 UV 的核心逻辑就是 **降维（3D -> 2D）与展开**。
这个过程通常被称为 **UV Unwrapping（展 UV）**。它的逻辑步骤如下：

### 1. 投影映射 (Projection)
最基础的生成逻辑就是“投影”。想象你在黑暗中拿手电筒照一个物体，物体在墙上的影子就是 2D 的。电脑也可以这样计算：
- **平面投影（Planar）**：从一个单一方向（比如正前方）把模型拍扁投影。适合墙面、地板等平整物体。
- **圆柱投影（Cylindrical）**：想象把一张纸卷成圆柱体包住模型，然后投影。适合树干、柱子、人的躯干等。
- **球体投影（Spherical）**：像地球仪的经纬网一样投影。适合球体或类似形状。

### 2. 切割接缝 (Marking Seams) - 复杂模型的核心逻辑
对于复杂的物体（比如一个人的模型、一辆车），简单的投影会产生严重的**拉伸（Distortion）**和**重叠（Overlap）**。
此时，生成逻辑就变成了像“裁缝做衣服的逆过程”：
1. **标记接缝（Seams）**：用户或算法需要在 3D 模型上画出一条条的“剪裁线”（比如衣服的侧边缝合处、纸盒的折痕处）。
2. **剥皮展开（Unwrapping/Peeling）**：电脑算法根据这些接缝，计算如何用最小的拉扯，把这层三维的“皮”摊平到一个 2D 的正方形画布（通常坐标范围是 0 到 1）上。
   - 底层算法（如 LSCM - 最小二乘共形映射，或 ABF - 角度边界平滑）会通过数学公式优化各个面的相对位置，**确保展开后的每个面（三角形）在 2D 空间中的面积比例和夹角，尽可能贴近其在 3D 空间中的原始状态**。
   - 如果不切开或者切的位置不对，展开的 UV 就会像强行把一个完整的橘子皮按平一样，必然会撕裂或严重变形。

### 3. 打包与布局 (Packing & Layout)
展开成一片片的“UV 岛”（UV Islands，也就是被切开后摊平的各个部分）后，生成逻辑的最后一步是将它们合理地塞进一个 0 到 1 的正方形坐标系（UV Space）内。
- **最大化利用空间**：像玩俄罗斯方块一样把它们紧凑地排列，确保空白区域最少，不浪费贴图的像素（空间利用率决定了贴图的清晰度）。
- **统一纹理密度（Texel Density）**：保证模型不同部位的贴图清晰度一致。比如脸部和衣服的 UV 岛大小比例，应该与它们在真实 3D 世界中的表面积比例大致相仿。

---

## 终极总结

- **它是什么**：UV 是用于 2D 纹理贴图的二维坐标系（水平为U，垂直为V）。
- **它的作用**：它是连接 3D 模型和 2D 图片的桥梁。它明确告诉计算机，**3D 模型表面上的每一个点，应该显示二维图片上哪一个具体位置的颜色或数据**。它是模型拥有外观材质、细节和灵魂的绝对先决条件。
- **生成逻辑**：本质上是将 3D 表面像剥橘子皮一样“剪开”并用数学算法“摊平”到 2D 平面上的过程。目标是使得 2D 展开后的形状尽可能不拉伸、不重叠，并像拼图一样高效紧凑地塞满一个正方形空间。

---

## 本工程（Pixel Map Maker）中的 UV 生成逻辑

在当前项目中，涉及 UV 生成的核心逻辑位于 `react-app/src/App.tsx` 文件中的 `expandLineToMesh` 函数。该函数的作用是将一根由一系列点构成的线（Polyline），沿着其法线方向向两侧扩展，生成具有一定宽度的“2D网格（Mesh）”，以便通过 WebGL 渲染出有粗细的线条或道路。

为了能够在这条生成出来的有宽度的线段上贴上纹理，代码通过数学计算动态生成了每个顶点的 UV 坐标。具体逻辑如下：

### 1. 确定 U 轴：沿着线条的前进方向映射
- **计算总长度**：代码首先遍历组成线段的所有点，计算出这条线的“总物理长度（`totalLength`）”。
- **计算当前进度（U坐标）**：在生成每个顶点时，代码会累加记录当前点距离起点的“已走过长度（`currentLength`）”。
- **映射到 0~1 范围**：利用公式 `u = currentLength / totalLength`，将进度转化为 0 到 1 之间的小数。
  - 起点处的 `U = 0`。
  - 中点处的 `U = 0.5`。
  - 终点处的 `U = 1`。

### 2. 确定 V 轴：横跨线条的宽度方向映射
- 在将一条线变宽时，原本的 1 个点会根据法线方向（与线条走向垂直的方向）被拆分成“左侧顶点”和“右侧顶点”。
- 为了让纹理能够完整横跨整条线的宽度，代码直接将这两侧的顶点固定映射到 V 轴的两端：
  - **左侧顶点的 V 坐标** 设为 `0`。
  - **右侧顶点的 V 坐标** 设为 `1`。

### 3. 代码中的对应体现
结合上述逻辑，在生成每个点对应的左右两个顶点（这会转化为 WebGL 所需的坐标数组）时，代码同步写入了它们的 UV 坐标：
```typescript
// uvs 数组依次存入: 左顶点的 (u, v); 右顶点的 (u, v)
// v 坐标分别为 0 和 1
const u = totalLength > 0 ? currentLength / totalLength : 0;
uvs.push(u, 0, u, 1);
```

### 总结
本工程使用了典型的**“样条线/路径展开（Spline Unwrapping）”逻辑**：
- **U坐标（沿着线走）** 决定了纹理在路径方向上的拉伸程度。通过计算“已走路程百分比”，确保了无论线条中间的折点多密集，纹理都能随着实际距离均匀地铺设，不会在点密集的地方被异常压缩。
- **V坐标（垂直于线走）** 决定了纹理横穿线条宽度的方向。左侧始终为 0，右侧始终为 1。

这就使得 WebGL 可以完美地把一张平面的道路或轨迹纹理，顺滑地、不扭曲地沿着任意弯曲复杂的路线“刷”出来。