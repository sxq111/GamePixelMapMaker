# 线条识别逻辑详解

## 一、整体流程概览

整个线条识别管线分为六个阶段：

```
图片加载 → 像素分类 → 连通分量检测 → 方向感知排序 → RDP 简化 → 网格/碰撞体生成
```

所有核心代码位于 `react-app/src/App.tsx`，入口函数为 `analyzeImage()`（第 726 行）。

---

## 二、像素分类（analyzeImage）

### 2.1 图片布局

输入图片为 2048×1024 像素，分为左右两半：

```
┌──────────────────────┬──────────────────────┐
│   左半 (1024×1024)    │   右半 (1024×1024)    │
│   地图数据            │   道具图              │
│   - 黑色像素: 墙壁    │   - 非纯白像素: 道具   │
│   - 绿色像素: 起点    │                      │
│   - 红色像素: 终点    │                      │
└──────────────────────┴──────────────────────┘
```

### 2.2 逐像素扫描

```typescript
const mapWidth = img.width > 1024 ? Math.floor(img.width / 2) : img.width

for (let y = 0; y < img.height; y++) {
  for (let x = 0; x < img.width; x++) {
    const idx = (y * img.width + x) * 4
    const [r, g, b, a] = [data[idx], data[idx+1], data[idx+2], data[idx+3]]

    if (x < mapWidth) {
      // 左半：地图分析
      if (r === 0 && g === 0 && b === 0)         → 墙壁点
      else if (r === 0 && g === 255 && b === 0)  → 起点
      else if (r === 255 && g === 0 && b === 0)  → 终点
    } else {
      // 右半：道具分析
      if (a > 0 && !(r===255 && g===255 && b===255)) → 道具
    }
  }
}
```

墙壁像素被存入两个数据结构：
- `wallPoints: Point[]` — 数组，用于后续的包围盒计算
- `wallSet: Set<string>` — 集合（key 为 `"x,y"`），用于快速查找和连通性分析

### 2.3 坐标归一化

像素分类完成后，计算所有点的最小 x/y（包围盒左上角），然后将所有坐标减去这个偏移量，使地图贴紧原点。

---

## 三、连通分量检测（traceLines 第一阶段）

### 3.1 目的

将散乱的墙壁像素点分组为独立的"线条"——每个连通分量对应地图中的一条连续墙壁。

### 3.2 8 方向邻接

两个黑色像素如果在 8 个方向（上下左右 + 四个对角）上相邻，则认为它们连通：

```
↖ ↑ ↗
← P →      P = 当前像素，共 8 个邻居方向
↙ ↓ ↘
```

### 3.3 DFS 实现

```typescript
const findConnectedComponent = (startX, startY) => {
  const component = []
  const stack = [{ x: startX, y: startY }]
  visited.add(`${startX},${startY}`)

  while (stack.length > 0) {
    const current = stack.pop()!
    component.push(current)

    for (const neighbor of getNeighbors(current.x, current.y)) {
      visited.add(`${neighbor.x},${neighbor.y}`)
      stack.push(neighbor)
    }
  }
  return component
}
```

使用栈实现的深度优先搜索（DFS），从任意一个未访问的墙壁像素出发，遍历所有与其连通的像素，得到一个连通分量。

### 3.4 遍历所有分量

```typescript
for (const key of wallSet) {
  if (!visited.has(key)) {
    const [x, y] = key.split(',').map(Number)
    const component = findConnectedComponent(x, y)
    if (component.length > 0) {
      const orderedLine = orderLinePoints(component)  // 进入第二阶段
      lines.push(orderedLine)
    }
  }
}
```

**注意：DFS 得到的 component 中的点是无序的，不能直接用于绘制。**

---

## 四、方向感知排序（traceLines 第二阶段 — 核心算法）

### 4.1 问题背景

当线条弯折回来时（如钩子形、S 形曲线），曲线的不同部分在像素级别上可能成为 8 方向邻居：

```
        A ← 曲线上半部分
        ↑
        B ← 曲线下半部分

A 和 B 虽然在曲线上相距很远，但在像素上可能是对角线邻居！
```

如果不考虑方向，追踪到 A 时可能错误地跳到 B，导致线条绘制混乱。

### 4.2 邻接表构建

先为连通分量中的每个点构建邻接表（记录该点在分量内的所有邻居）：

```typescript
const adjacency = new Map<string, Point[]>()
for (const p of component) {
  const key = `${p.x},${p.y}`
  adjacency.set(key, [])
  for (const [dx, dy] of 8个方向) {
    const nkey = `${p.x + dx},${p.y + dy}`
    if (pointSet.has(nkey)) {
      adjacency.get(key)!.push({ x: p.x + dx, y: p.y + dy })
    }
  }
}
```

```
示例:
  A — B — C        A: [B]
      |             B: [A, C, D]
      D — E         C: [B]
                    D: [B, E]
                    E: [D]
```

### 4.3 找端点作为起点

遍历分量中的所有点，找到邻居数最少的点作为追踪起点：

```typescript
let lineStart = component[0]
let minNeighbors = 9
for (const p of component) {
  const count = adjacency.get(`${p.x},${p.y}`)!.length
  if (count < minNeighbors) {
    minNeighbors = count
    lineStart = p
    if (count === 1) break  // 真正的端点（只有 1 个邻居）
  }
}
```

- 邻居数为 1 的点是线条端点（最理想的情况）
- 闭合曲线中所有点邻居数都 >= 2，则退回到选择分量中的第一个点

### 4.4 方向感知追踪

核心思想：**在选择下一个点时，选择与当前移动方向最一致的邻居。**

```typescript
const ordered = [lineStart]
let lastDir: number | null = null  // 上一步的移动方向（弧度）

while (ordered.length < component.length) {
  const current = ordered[ordered.length - 1]
  const neighbors = adjacency.get(`${current.x},${current.y}`) || []
  const unvisited = neighbors.filter(n => !visitedOrder.has(`${n.x},${n.y}`))

  if (unvisited.length === 0) break

  let bestNeighbor: Point | null = null

  if (lastDir === null || unvisited.length === 1) {
    bestNeighbor = unvisited[0]  // 第一步或只有一个选择，直接选
  } else {
    // 选择方向变化最小的邻居
    let minDiff = Infinity
    for (const n of unvisited) {
      const newDir = Math.atan2(n.y - current.y, n.x - current.x)
      const diff = angleDiff(lastDir, newDir)
      if (diff < minDiff) {
        minDiff = diff
        bestNeighbor = n
      }
    }
  }

  lastDir = Math.atan2(bestNeighbor.y - current.y, bestNeighbor.x - current.x)
  ordered.push(bestNeighbor)
}
```

### 4.5 角度差计算

```typescript
const angleDiff = (a1, a2) => {
  let diff = a2 - a1
  while (diff > Math.PI)  diff -= 2 * Math.PI   // 归一化到 [-π, π]
  while (diff < -Math.PI) diff += 2 * Math.PI
  return Math.abs(diff)
}
```

角度示意图：

```
              -π/2 (向上)
                ↑
   π (向左) ←———+———→ 0 (向右)
                ↓
              π/2 (向下)
```

### 4.6 算法执行示例

```
曲线:  1—2—3
           |
       7—6—4
           |
           5

追踪过程:
  步骤  当前点  lastDir    未访问邻居   各方向         选择
  ────────────────────────────────────────────────────
   1     1      null       [2]          —              2
   2     2      0°(→)      [3]          —              3
   3     3      0°(→)      [4]          90°(↓)         4
   4     4      90°(↓)     [5,6]        90°(↓),180°(←) 5 ← 更接近当前方向
   5     5      90°(↓)     [6]          —              6
   6     6      180°(←)    [7]          —              7

结果: [1, 2, 3, 4, 5, 6, 7]  ✓ 自然顺序
```

---

## 五、RDP 线条简化（Rammer-Douglas-Peucker）

### 5.1 目的

线序追踪得到的是像素级精度的点序列，相邻点在一条直线上会产生大量冗余点。RDP 算法在保留形状特征的前提下，大幅减少点数。

### 5.2 算法原理

1. 连接首尾两点 A、B，形成一条基准线
2. 计算中间所有点到这条基准线的垂直距离
3. 找到距离最大的点 C
4. 如果 C 的距离 > ε（阈值），则递归处理 A→C 和 C→B 两段
5. 如果所有点的距离都 ≤ ε，则只保留 A、B 两个端点

```
迭代 1:  A ··· C ··· B    距离 C 最大 > ε → 分为 A-C 和 C-B
迭代 2:  A · C            距离 < ε → 保留 A, C
迭代 2:  C · B            距离 < ε → 保留 C, B

结果:  [A, C, B]
```

### 5.3 实现

```typescript
const rdpSimplify = (points, epsilon) => {
  if (points.length <= 2) return points

  // 找距离首尾连线最远的点
  let maxDist = 0, maxIndex = 0
  for (let i = 1; i < points.length - 1; i++) {
    const dist = perpendicularDistance(points[i], points[0], points[points.length - 1])
    if (dist > maxDist) { maxDist = dist; maxIndex = i }
  }

  if (maxDist > epsilon) {
    const left  = rdpSimplify(points.slice(0, maxIndex + 1), epsilon)
    const right = rdpSimplify(points.slice(maxIndex), epsilon)
    return [...left.slice(0, -1), ...right]  // 去重连接点
  } else {
    return [points[0], points[points.length - 1]]  // 只保留端点
  }
}
```

### 5.4 点到线段垂直距离

```typescript
const perpendicularDistance = (point, lineStart, lineEnd) => {
  const dx = lineEnd.x - lineStart.x
  const dy = lineEnd.y - lineStart.y
  if (dx === 0 && dy === 0) {
    return Math.sqrt((point.x - lineStart.x) ** 2 + (point.y - lineStart.y) ** 2)
  }
  // 叉积公式
  const numerator = Math.abs(dy * point.x - dx * point.y
    + lineEnd.x * lineStart.y - lineEnd.y * lineStart.x)
  const denominator = Math.sqrt(dx * dx + dy * dy)
  return numerator / denominator
}
```

### 5.5 参数选择

项目中 ε = 1.0（像素），这意味着与基准线偏差 ≤ 1 像素的点被视为共线并被移除。

---

## 六、闭合曲线检测（isLineClosed）

判断一条线的首尾是否在 8 方向邻接范围内：

```typescript
const isLineClosed = (points) => {
  if (points.length < 3) return false
  const dx = Math.abs(points[0].x - points[points.length - 1].x)
  const dy = Math.abs(points[0].y - points[points.length - 1].y)
  return dx <= 1 && dy <= 1  // 包括水平和垂直相邻、以及对角相邻
}
```

条件：首尾点距离 ≤ √2（即 dx ≤ 1 且 dy ≤ 1），意味着 8 方向内的邻居关系就算闭合。

---

## 七、碰撞多边形生成（generatePolygons）

### 7.1 目的

将简化后的 1px 细线条扩展为有厚度的多边形，用于游戏中的碰撞检测。

### 7.2 斜接连接（Miter Join）算法

对线条上的每个点，计算其两侧的垂线偏移：

```
对于中间点（有前驱和后继）:
  1. 计算入边方向向量 d1（prev → curr）和出边方向向量 d2（curr → next）
  2. 分别计算两条边的法向量 n1 = (-dy1/len1, dx1/len1) 和 n2 = (-dy2/len2, dx2/len2)
  3. 取平均法向量 perp = normalize(n1 + n2)
  4. 用点积修正确保斜接长度一致
  5. leftSide  = curr + perp * halfThickness
     rightSide = curr - perp * halfThickness

对于端点:
  直接使用所在边的法向量（因为没有入边或出边）
```

### 7.3 多边形拼接

**开放线条**：将 leftSide + rightSide.reverse() 拼接为一个包裹多边形

```
  leftSide:  L0——L1——L2——L3
             |             |
  rightSide: R0——R1——R2——R3
             拼接后: [L0, L1, L2, L3, R3, R2, R1, R0]
```

**闭合线条**：生成两个多边形
- 主体（Main Body）：leftSide + rightSide.reverse()，从点 0 到点 n-1，形成一个 C 形环
- 桥接填充（Bridge Filler）：连接首尾端点的四个角点 [leftSide[n-1], leftSide[0], rightSide[0], rightSide[n-1]]，用于闭合缺口

```
  主体多边形: L0—L1—L2—L3—R3—R2—R1—R0
                                   (开口在 L3-R3 和 L0-R0 之间)

  桥接填充:    L3————L0
              |        |
              R3————R0           ← 四个点闭合缺口
```

厚度参数：2px，与网格的 1px 厚度（halfThickness * 2 = 1 * 2 = 2px）匹配。

---

## 八、网格生成（expandLineToMesh）

### 8.1 目的

为每条简化线生成三角形网格数据，供 WebGL / Cocos Creator 渲染使用。

### 8.2 UV 坐标

- **U 坐标**：沿线条长度方向，从 0（起点）到 1（终点），逐段累加计算
- **V 坐标**：垂直方向，0 表示线条左侧，1 表示右侧

```
  线条左侧 (V=0):  L0——L1——L2——L3
                   |  /|  /|  /|
                   | / | / | / |
  线条右侧 (V=1):  R0——R1——R2——R3

  每个四边形由两个三角形组成:
    三角形 1: (L0, R0, L1)  — 左上、左下、右上
    三角形 2: (R0, R1, L1)  — 左下、右下、右上
```

### 8.3 数据结构

```typescript
interface LineMesh {
  vertices: number[]   // [x, y, x, y, ...]，每两个 float 一组
  uvs: number[]        // [u, v, u, v, ...]
  indices: number[]    // 三角形索引
  closed: boolean
}
```

### 8.4 总长度计算

```typescript
let totalLength = 0
for (let i = 1; i < n; i++) {
  totalLength += dist(points[i], points[i - 1])
}
if (closed) {
  totalLength += dist(points[0], points[n - 1])  // 闭合段
}
```

---

## 九、完整数据流

```
                  ┌─────────────────────┐
                  │   HTML Canvas        │
                  │   getImageData()     │
                  │   → Uint8ClampedArray │
                  └──────────┬──────────┘
                             │
                             ▼
                  ┌─────────────────────┐
                  │   逐像素 RGB 分类    │
                  │   wallSet, items    │
                  └──────────┬──────────┘
                             │
                             ▼
                  ┌─────────────────────┐
                  │   traceLines()      │
                  │   ┌───────────────┐ │
                  │   │ DFS 连通分量   │ │
                  │   │ 8方向邻接      │ │
                  │   └───────┬───────┘ │
                  │           ▼         │
                  │   ┌───────────────┐ │
                  │   │ 方向感知排序   │ │
                  │   │ endPoint →     │ │
                  │   │ atan2 +        │ │
                  │   │ angleDiff      │ │
                  │   └───────────────┘ │
                  └──────────┬──────────┘
                             │  lines: Point[][]
                             ▼
                  ┌─────────────────────┐
                  │   rdpSimplify()     │
                  │   ε = 1.0           │
                  └──────────┬──────────┘
                             │  simplifiedLines
                             ▼
          ┌──────────────────┼──────────────────┐
          ▼                                     ▼
┌──────────────────┐                 ┌──────────────────┐
│ expandLineToMesh │                 │ generatePolygons │
│ UV坐标 + 三角形   │                 │ 碰撞多边形        │
│ 网格数据          │                 │ （斜接连接）      │
└──────────────────┘                 └──────────────────┘
          │                                     │
          ▼                                     ▼
   WebGL 渲染 (网格视图)              Canvas 2D 渲染 (碰撞视图)
```

---

## 十、关键参数汇总

| 参数 | 值 | 位置 | 说明 |
|------|-----|------|------|
| 邻接方向 | 8 方向 | `traceLines()` | DFS 和邻接表构建均使用 |
| RDP ε | 1.0 | `analyzeImage()` | 简化容差，1px 精度 |
| 网格厚度 | 1px | `generateMeshes()` | halfThickness = 0.5 |
| 碰撞体厚度 | 2px | `simplifyLines()` | halfThickness = 1.0 |
| 图片宽度 | 2048px | `analyzeImage()` | 左右各 1024px |
| 闭合判定距离 | ≤ √2 (dx≤1, dy≤1) | `isLineClosed()` | 8 方向邻接即闭合 |

---

## 十一、算法复杂度

| 阶段 | 时间复杂度 | 空间复杂度 |
|------|-----------|-----------|
| 像素分类 | O(W×H) | O(n) |
| DFS 连通分量 | O(n) | O(n) |
| 方向感知排序 | O(n) | O(n) |
| RDP 简化 | O(m log m) | O(m) |
| 网格生成 | O(m) | O(m) |
| 碰撞体生成 | O(m) | O(m) |

其中 n = 墙壁像素总数，m = 简化后点数（m ≪ n），W×H = 图片分辨率。
