实现一个页面，由以下结构组成

1. 页面顶部一个按钮，点击选取一张本地图片。我们假设这个图片是1024 * 1024的大小。这个图片是我绘制的地图数据
2. 选择图片后，按钮下方div中展示出这个图片，展示原图，不要压缩
3. 选择图片后，分析图片中的数据，#fff为背景色，#000为墙壁，#0f0为起点，#f00为终点，你需要分析出以下数据
    1. 本地图中所有墙壁点的坐标数组
    2. 本地图中起点位置
    3. 本地图中终点位置
    4. 本地图中墙壁组成的连续线条的条数，以及线条组成点的数组（线条中的点不能乱序，要可以直接用这些点画出线条）
 4. 在下方展示一个canvas，1024 * 1024大小，背景白色，用黑色（1px）画出线条，红色圆圈（3px大小）画出终点，绿色（3px）画出起点
 5. 分析数据的json输出到控制台
   